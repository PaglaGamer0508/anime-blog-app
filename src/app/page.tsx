import Link from "next/link";
import React from "react";

interface pageProps {}

const page: React.FC<pageProps> = ({}) => {
  return (
    <div>
      <h1>home page</h1>
    </div>
  );
};

export default page;
